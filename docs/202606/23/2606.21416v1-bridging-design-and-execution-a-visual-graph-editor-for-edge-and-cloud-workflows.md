---
title: "Bridging Design and Execution: A Visual Graph Editor for Edge and Cloud Workflows"
title_zh: 桥接设计与执行：面向边缘和云工作流的可视化图形编辑器
authors: "Katarina-Glorija Grujić, Nikola Stanković, Maja Vukasović, Miloš Simić"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21416v1"
tags: ["query:eca"]
score: 8.0
evidence: 面向边缘和云工作流的可视化图形编辑器
tldr: 边缘和云计算应用需要协调多个计算内核、共享数据和事件驱动执行。本文提出一个域特定可视化图形编辑器，支持内核、共享内存节点和事件触发器三种抽象，用户可图形化构建并配置节点。编辑器自动将图序列化为JSON/XML并传递给执行API，桥接设计与部署。通过联邦学习工作流示例，展示了其在数据共享与事件协调方面的实用性，为分布式环境下的模块化计算编排提供了可扩展的基础设施。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21416v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1244, \"height\": 681, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21416v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1031, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21416v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 741, \"height\": 634, \"label\": \"Figure\"}]"
motivation: 缺乏统一工具来建模和协调边缘云环境中多个内核、共享数据及事件驱动的执行依赖。
method: 开发可视化图形编辑器，支持内核、共享内存节点和事件触发器作为一等抽象，自动生成JSON/XML表示并连接执行API。
result: 图形化建模可序列化为机器可读格式并直接部署，联邦学习工作流验证了数据共享与事件协调的有效性。
conclusion: 为分布式模块化计算编排提供可视化基础，支持用户自定义内核和事件类型，可扩展至多种执行后端。
---

## 摘要
设计面向边缘和云计算环境的模块化应用程序需要协调多个计算内核、共享数据和事件驱动执行。本文介绍了一个领域特定的可视化图形编辑器，使用户能够通过统一界面建模此类应用。该编辑器支持三种一等抽象：内核（表示计算单元）、共享内存节点（建模分布式数据）以及事件触发器（捕获执行依赖关系）。用户可以直观地构建图形、配置节点属性并连接元素以定义数据流和控制流。生成的图形会自动序列化为机器可读表示（JSON/XML），并可传递给执行API，从而弥合设计时建模与运行时部署之间的差距。编辑器的基于图形的模型改善了数据共享、执行顺序和依赖关系的推理，特别是在分布式边缘和云场景中。为了展示其适用性，我们讨论了一个联邦学习工作流，其中本地训练内核通过事件驱动协调与共享全局模型交互。与传统工作流编辑器和通用图表工具相比，所提出的系统提供了明确的执行语义、模块化和直接可部署性。这项工作为分布式环境中模块化计算的视觉编排奠定了基础，并为用户定义的内核、事件类型和替代执行后端提供了可扩展性，从而支持未来复杂分布式应用的探索。

## Abstract
Designing modular applications for edge and cloud computing environments involves coordinating multiple computational kernels, shared data, and event-driven execution. This paper presents a domain-specific visual graph editor that enables users to model such applications using a unified interface. The editor supports three first-class abstractions: kernels, representing computational units; shared memory nodes, modeling distributed data; and event triggers, capturing execution dependencies. Users can construct graphs visually, configure node properties, and connect elements to define data and control flow. The resulting graphs are automatically serialized into machine-readable representations (JSON/XML) and can be passed to an execution API, bridging the gap between design-time modeling and runtime deployment. The editor's graph-based model improves reasoning about data sharing, execution order, and dependencies, particularly in distributed edge and cloud scenarios. To demonstrate its applicability, we discuss a federated learning workflow, where local training kernels interact with a shared global model through event-driven coordination. Compared to traditional workflow editors and general-purpose diagramming tools, the proposed system provides explicit execution semantics, modularity, and direct deployability. This work lays the foundation for visual orchestration of modular computation in distributed environments and offers extensibility for user-defined kernels, event types, and alternative execution backends, enabling future exploration of complex distributed applications.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：边缘和云计算应用日益采用模块化、分布式架构，由多个计算内核（kernels）通过共享数据和事件驱动执行进行协调。然而，现有工作流编排工具（如 Apache Airflow、Argo Workflows）或通用图表工具（如 draw.io）缺乏对**显式共享分布式内存**和**事件驱动语义**的专门抽象，导致设计时模型与运行时部署脱节，用户需手动将视觉模型转换为可执行规格。
- **核心问题**：缺乏一种**统一、可视、可直接执行**的建模工具，能够同时表示计算单元、数据共享和事件依赖，从而降低分布式应用编排的复杂性，并弥合设计与执行之间的鸿沟。
- **整体含义**：本文提出的领域特定可视化图形编辑器，通过引入三类一等抽象（内核、共享内存节点、事件触发器），使开发者能够以图形方式构建并自动生成可执行规格，为边缘-云环境中模块化计算的视觉编排奠定基础。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将应用程序建模为**有向类型图** $G=(V,E)$，其中顶点 $v \in V$ 分为三类——**内核节点**（表示外部定义的计算单元）、**内存节点**（表示共享分布式数据）、**事件触发器节点**（表示执行依赖条件）。有向边表示数据流或控制流，并区分**硬链接**（写/所有权）和**软链接**（只读访问）。
- **关键技术细节**：
  - **内核节点**：关联唯一标识符、外部实现引用、参数列表；从输入边消费数据，执行计算，在输出边产生数据/事件。
  - **内存节点**：关联逻辑标识符、访问模式、一致性级别等配置；多条边连接多个内核，支持多对多数据访问。
  - **事件触发器节点**：关联事件类型、触发条件、过滤规则；解耦事件生产与消费，支持异步、反应式模式。
  - **边语义**：硬链接（实线）表示数据所有权，软链接（虚线）表示只读访问。
  - **可执行表示**：图形自动序列化为 JSON/XML，保留拓扑和语义属性，可传递给执行 API 实例化内核、配置内存并协调执行。
  - **编辑器交互**：Web 应用，三种节点使用不同形状（矩形、圆角矩形、圆柱体等），通过拖拽构建图形，实时验证连接合法性，提供属性配置面板。

## 3. 实验设计

- **数据集/场景**：论文**未设计定量实验**，仅以**联邦学习工作流**作为用例进行概念验证。该用例描述了本地训练内核、全局模型共享内存、聚合事件触发器的交互逻辑。
- **Benchmark**：无对比基准（如与传统工具比较代码行数、建模时间等）。
- **对比方法**：文中提及与通用图表工具（draw.io、Visio）、工作流系统（Airflow、Argo）、边缘编排平台（K3s）等对比，但均为定性分析，未提供定量结果。

## 4. 资源与算力

- **未明确说明**：论文未提及使用任何 GPU 资源、训练时长或硬件平台。编辑器本身是 Web 应用，不涉及大规模计算资源。联邦学习用例仅为模型示意，未实际运行实验。

## 5. 实验数量与充分性

- **实验数量**：零组定量实验。仅展示了一个联邦学习图例（图3）和配置界面截图（图1、图2），无消融实验、性能评估或用户研究。
- **充分性**：**非常不足**。论文属于概念提议+工具介绍阶段，缺乏实证证据证明编辑器在实际使用中的效率提升、正确性或可扩展性。未对比现有工具的设计时间、错误率或用户满意度。

## 6. 论文的主要结论与发现

- 提出了一种**领域特定可视化图形编辑器**，支持显式建模计算（内核）、数据（共享内存）和执行依赖（事件触发器）。
- 通过**自动序列化为可执行规格**（JSON/XML），实现了设计到部署的桥接，无需手动翻译。
- 联邦学习用例展示了编辑器在表达**分布式协调、数据局部性和事件循环**方面的直观性。
- 编辑器具有**可扩展性**，支持用户自定义内核、事件类型和不同执行后端。

## 7. 优点

- **统一抽象**：将内核、共享内存、事件触发作为一等公民，使数据共享和依赖关系显式化，有利于推理。
- **直接可执行**：图形模型自动转为机器可读规格，消除了设计与部署之间的手动转换步骤。
- **可视化和交互友好**：不同节点使用不同形状、边用实/虚线区分访问权限，实时验证防止语义错误。
- **模块化与可扩展性**：支持添加自定义内核、事件类型，适配不同执行后端。

## 8. 不足与局限

- **缺乏定量评估**：没有用户研究、性能测试、可用性测试或与现有工具的量化对比，无法证明其实际优势。
- **执行后端未实现**：文中仅提出可传递至执行API，但未展示真实部署或运行时行为，联邦学习用例仅为示意图。
- **事件类型有限**：当前事件触发器仅覆盖内部事件，外部或用户定义事件的支持仅在计划中。
- **未提供开源实现或代码**：论文未提供编辑器源码或可运行的演示，不利于复现和社区验证。
- **应用范围有限**：仅针对“内核-共享内存-事件”模型，可能不适用于其他类型的分布式流程（如纯流式处理、服务网格）。
- **验证环境缺失**：未讨论边缘/云异构性、节点动态加入/离开等实际挑战。

（完）
