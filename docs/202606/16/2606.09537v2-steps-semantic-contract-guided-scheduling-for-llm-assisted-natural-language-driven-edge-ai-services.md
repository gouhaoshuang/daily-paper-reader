---
title: "STEPS: Semantic Contract-Guided Scheduling for LLM-Assisted Natural Language-Driven Edge AI Services"
title_zh: STEPS：面向LLM辅助自然语言驱动边缘AI服务的语义契约引导调度
authors: "Houyi Qi, Minghui Liwang, Xianbin Wang, Xinlei Yi, Seyyedali Hosseinalipour"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09537v2"
tags: ["query:eca"]
score: 9.0
evidence: 基于语义契约的边缘AI服务调度
tldr: 现有边缘调度框架假设服务需求为数值约束，无法处理用户自然语言表达的模糊意图。本文提出STEPS，引入语义合同作为可执行接口，用LLM解析自然语言并提取语义需求与置信度，将调度建模为合同引导的势博弈，联合优化节点选择、计算和带宽资源，并通过反馈实现自适应。实验证明STEPS显著提升语义合同履行率、降低服务损失，在非平稳网络环境中保持鲁棒适应。该工作弥合了语义到优化的鸿沟，为自然语言驱动的边缘AI服务调度提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09537v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 917, \"height\": 564, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09537v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 880, \"height\": 1025, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09537v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1847, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09537v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1830, \"height\": 348, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09537v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1850, \"height\": 332, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09537v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 908, \"height\": 592, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09537v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 850, \"height\": 137, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09537v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 859, \"height\": 214, \"label\": \"Table\"}]"
motivation: 用户自然语言需求与边缘调度数值约束之间存在语义鸿沟，现有方法无法有效处理模糊需求。
method: 提出STEPS，引入语义合同作为可执行接口，利用LLM解析自然语言提取语义需求与置信度，将调度建模为势博弈并联合优化，支持反馈自适应。
result: 实验表明STEPS提升语义合同履行率，降低服务损失，在非稳定网络环境中鲁棒适应。
conclusion: STEPS有效弥合语义到优化鸿沟，为自然语言驱动的边缘AI服务调度提供新范式。
---

## 摘要
边缘用户/服务调度已成为分布式AI系统的基石，决定了在有限通信和计算资源下AI服务的执行位置和方式。现有的边缘调度框架通常假设服务需求以数值约束形式给出，例如延迟界限或能量预算。然而，在实际中，用户通常通过模糊且依赖上下文的自然语言表达服务期望，这造成了用户意图与调度决策之间的鸿沟。为弥合这一语义到优化的差距，我们提出语义契约引导的边缘潜在调度（STEPS），一个自然语言驱动的调度框架，引入语义契约作为用户侧语义与边缘侧决策之间的可执行接口。在STEPS中，大语言模型（LLM）辅助解析器解释自然语言请求，提取带有置信度分数的语义服务需求，这些需求被转化为服务需求和语义不确定性。基于此信息，STEPS将边缘调度形式化为一个契约引导的潜在博弈，联合决定执行节点选择、计算资源供应和带宽分配。STEPS进一步利用反馈信号支持在演变的服务和网络条件下的自适应调度。我们刻画了该精确潜在博弈结构，建立了纯策略纳什均衡的存在性，并证明了调度与自适应过程的收敛性和稳定性。大量实验表明，STEPS在非平稳网络化AI环境中，能够提高语义契约履行率，减少契约引导的服务损失，并在模糊的自然语言请求下保持鲁棒的自适应能力。

## Abstract
Edge user/service scheduling has become a cornerstone of distributed AI systems, determining where and how AI services are executed under limited communication and computing resources. Existing edge scheduling frameworks usually assume that service requirements are given as numerical constraints, such as latency bounds or energy budgets. In practice, users often express service expectations through ambiguous and context-dependent natural language, creating a gap between user intent and scheduling decisions. To bridge this semantic-to-optimization gap, we propose semantic contract-guided edge potential scheduling (STEPS), a natural language-driven scheduling framework that introduces semantic contracts as executable interfaces between user-side semantics and edge-side decision making. In STEPS, a large language model (LLM)-assisted parser interprets natural language requests and extracts semantic service requirements with confidence scores, which are converted into service requirements and semantic uncertainty. Based on this information, STEPS formulates edge scheduling as a contract-guided potential game that jointly determines execution-node selection, computing-resource provisioning, and bandwidth allocation. STEPS further uses feedback signals to support adaptive scheduling under evolving service and network conditions. We characterize the exact potential game structure, establish the existence of a pure-strategy Nash equilibrium, and prove convergence and stability properties of the scheduling and adaptation processes. Extensive experiments show that STEPS improves semantic contract fulfillment, reduces contract-guided service loss, and maintains robust adaptation under ambiguous natural language requests in non-stationary networked AI environments.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：现有边缘调度框架通常假设用户的AI服务需求以数值形式（如延迟上限、能量预算）明确给出，但实际用户往往通过模糊、依赖上下文的自然语言表达期望（如“尽快完成”）。这造成了“用户语义意图”与“边缘调度优化决策”之间的语义鸿沟。同时，边缘环境动态变化（资源、执行条件时变），进一步加剧了这一挑战。
- **整体目标**：提出一个自然语言驱动的边缘AI服务调度框架，能够将模糊的自然语言请求转化为可执行、可优化的服务规范，并在非平稳环境下自适应调整调度策略，提升语义契约的履行率并减少服务损失。

### 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：引入**语义契约（Semantic Contract）**作为用户侧自然语言与边缘侧调度决策之间的可执行接口。通过LLM解析用户自然语言请求，提取语义需求（延迟/能量/成本/信任敏感度）及置信度，再结合系统上下文（资源、信道、队列等）生成包含偏好向量、履行界限和语义不确定性的语义契约。基于该契约，将边缘调度建模为**契约引导的潜在博弈**，联合优化执行节点选择、计算资源分配和带宽分配。执行后，根据履约反馈自适应调整调度参数（语义准入阈值、契约保守因子、边缘协调增益等），形成闭环。
- **关键技术细节**：
  - **LLM辅助语义解析**：由Qwen3.5-4B解析SED，输出语义特征向量（延迟、能量、成本、信任的敏感级别）及解析置信度。
  - **语义契约生成**：通过可解释映射规则将语义特征、任务属性和系统上下文转换为：偏好权值向量w（softmax归一化）、履行界限向量g（延迟/能量/成本上限、信任下限）、语义不确定性ρ（融合解析置信度低和上下文不一致风险）。
  - **校准与准入**：用保守因子β(t)和ρ对契约界限进行保守校准（使高不确定请求执行更严格界限）；仅允许ρ(t)_i ≤ 阈值τ_ρ(t)的请求参与调度。
  - **潜在博弈建模**：将每个时隙的调度问题转化为精确潜在博弈G(t)，玩家为已准入用户，动作空间为离散的资源包与执行节点组合。调度成本J(t)_i由合同引导服务损失H(t)_i（含归一化度量及违反惩罚）和边际拥塞惩罚组成。全局势函数Φ(t)为各用户服务损失之和加边缘拥塞惩罚。
  - **异步最优响应**：用户按异步顺序执行严格最优响应更新，保证势函数严格下降，有限步收敛到纯策略纳什均衡。
  - **资源实现**：博弈输出意图动作后，ESP进行按比例缩放以满足实际资源容量。
  - **反馈驱动自适应**：定义语义请求漂移Δ_sem、履约漂移Δ_ful、履约压力P_ful、准入压力P_adm，结合形成反馈信号Ψ_ful。据此更新资源价格λ、语义准入阈值τ_ρ、契约保守因子β、边缘协调增益χ，实现非平稳环境下的自适应。

### 实验设计：数据集、场景与对比方法

- **实验场景**：
  - **合成实验**：在800×800m²区域，8个边缘服务器（默认），50-300个用户设备。自然语言请求来自语义模板（涵盖时延、能量、成本、信任四个维度，每个维度低/中/高三档），同时注入比例r_unc∈[0,0.5]的模糊/冲突/越界请求模拟语义不确定性。非平稳性通过在第50个时隙引入语义偏好和工作负载漂移（漂移强度0.6）实现。
  - **真实数据集**：使用EUA数据集中的墨尔本中央商务区（CBD）拓扑（300个用户设备、12个边缘站点）。
- **对比方法**：
  - Param-Opt：传统参数驱动优化，假设已知数值需求。
  - Direct-LLM：LLM直接翻译自然语言为调度参数，不建模不确定性或合同校准。
  - DRL-Opt：混合深度强化学习与优化修复。
  - Intent-Reconf：违规触发的意图管理，后执行纠正。
  - GAH：广义分配启发式。
  - 消融变体：NoCal（无语义准入及不确定性校准）、NoEvo（固定参数无演化）。
- **性能指标**：平均履行度（AFD）、合同违规率（CVR）、平均合同引导服务损失（ACSL）、语义准入率（SAR）、平均服务延迟（ASD）、运行时间（RT）、漂移后累积平均ACSL（Post-ACSL）等。

### 资源与算力

- 论文明确说明：语义解析使用Qwen3.5-4B模型通过Ollama本地服务，在配备**12th Gen Intel Core i9-12900H CPU**和**NVIDIA GeForce RTX 3060 GPU**的工作站上运行。**未提及模型训练时间或GPU数量**，仅强调推理采用模板级缓存以提升效率。因此具体训练算力细节未给出，但推理阶段使用单张消费级GPU，算力需求较低。

### 实验数量与充分性

- **实验组数**：所有结果基于30次独立蒙特卡洛实验的平均值（除非另有说明）。共进行以下主要实验：
  1. 语义不确定性影响实验（r_unc从0到0.5共6个水平，对比6种方法，共约180次独立实验）。
  2. 问题规模可扩展性实验（4种规模S1-S4，对比6种方法，共约120次独立实验）。
  3. 非平稳漂移适应实验（1个漂移时间点，对比6种方法，连续100个时隙，30次重复，详细展示动态度量曲线）。
  4. 消融实验（3种变体，对比4个指标，基于30次重复，共约90次实验）。
  5. 真实数据集实验（对比6种方法，6个指标，30次重复，约180次实验）。
- **充分性评价**：实验涵盖了合成场景和真实场景，考虑了多种不确定性水平、不同问题规模、非平稳漂移，并进行了消融分析。对比基线覆盖了参数驱动、LLM直接使用、DRL、纠正式管理、启发式等多种类型，评估指标全面（包括履约、损失、延迟、开销等）。实验设计较为充分、客观、公平。但未进行跨不同LLM解析器的对比，也未在更多真实网络拓扑上验证，可能稍显局限。

### 论文的主要结论与发现

- **语义不确定性鲁棒性**：STEPS在提高r_unc时，AFD下降最缓、CVR最低、ACSL最低（与Direct-LLM相当但更稳定），且SAR仅轻度下降，说明通过语义合同和校准有效应对模糊请求。
- **可扩展性**：STEPS在不同问题规模下均保持高AFD、低ACSL，运行时间与Param-Opt、Direct-LLM相仿，优于DRL-Opt等，表明合同引导的潜在博弈在更大系统中计算可管理。
- **非平稳适应**：在漂移后，STEPS的ACSL和Post-ACSL最低，远低于DRL-Opt、Intent-Reconf和GAH，甚至优于Direct-LLM，说明反馈驱动的自适应更新（合同保守因子、协调增益等）有效提升了动态环境下的履约能力。
- **消融证实**：NoCal和NoEvo的性能均不如完整STEPS，证明不确定性校准和反馈演化的必要性。
- **真实数据集**：在Melbourne CBD拓扑上，STEPS获得最低ACSL和Post-ACSL，验证了在真实空间分布下的有效性。

### 优点：方法或实验设计上的亮点

- **创新性**：首次提出“语义契约”作为自然语言与边缘优化之间的可执行接口，填补了语义到优化鸿沟，且利用势博弈实现分布式调度，理论性质优美（精确势博弈、纯策略NE存在、有限步收敛、参数有界）。
- **闭环设计**：完整覆盖从自然语言解析→合同生成→博弈调度→执行→反馈→自适应更新，实现了语义理解的迭代优化。
- **实验全面**：同时采用了合成和真实数据集，对语义不确定性、规模、非平稳漂移及消融进行了系统评估，对比基线多样，结果统计可靠（30轮蒙特卡洛）。
- **可解释性**：合同参数由可解释映射规则生成而非完全依赖LLM输出，利于调试和可控性。

### 不足与局限

- **实验覆盖**：仅使用Qwen3.5-4B作为单一LLM解析器，未比较其他LLM（如GPT-4、Llama等）差异；真实数据集仅用一个城区拓扑（Melbourne CBD），泛化性有限。
- **语义不确定性建模**：不确定性度量基于简单加权和投影，可能未充分考虑语义歧义的类型多样性（如矛盾、缺失等）。
- **潜在博弈简化**：动作空间依赖资源包离散化，可能丢失最优连续解；资源实现采用按比例缩放，对公平性未作进一步考量。
- **在线开销**：虽然运行时间可接受，但相比GAH等启发式方法仍显较高，对于毫秒级超低延迟场景可能不适用。
- **未讨论安全与隐私**：自然语言请求可能涉及敏感信息，LLM解析引入隐私风险，论文未涉及。
- **反馈信号权重依赖人工设置**：α_s、α_f、α_p等需人工调节，实际部署中可能需要自动调参机制。

（完）
