---
title: "tap: A File-Based Protocol for Heterogeneous LLM Agent Collaboration"
title_zh: tap：一种基于文件的异构LLM智能体协作协议
authors: Minseo Kim
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14445v1"
tags: ["query:eca"]
score: 9.0
evidence: 面向异构LLM代理的基于文件的协作协议
tldr: "现有多智能体系统假设共同运行时或相同API，导致不同厂商的LLM代理难以直接协作。tap协议采用文件优先设计，将消息保存为带元数据的markdown文件，结合文件检查与实时通知路径，并通过git工作树隔离工作。在27天自应用实验中，异构模型对（Claude+Codex）的审查缺陷率达69.8%，高于同质模型对的53.1%。该协议无需共享内存即可实现异构LLM代理协作，已开源为npm包。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14445v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 677, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14445v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 542, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14445v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 947, \"height\": 250, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14445v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 902, \"height\": 403, \"label\": \"Table\"}]"
motivation: 现有系统无法让不同厂商的LLM代理在各自独立执行环境中直接交换消息并协作开发代码。
method: tap采用文件优先架构，以markdown文件持久化消息，结合文件检查（Tier1）和实时通知（Tier2），并用git工作树隔离单任务工作。
result: "27天自应用实验中，异构模型对（Claude+Codex）的审查缺陷率为69.8%，高于同质模型对的53.1%，证明异构协作拓展了视角。"
conclusion: 基于文件的协议结合实时通知，支持异构LLM代理在共享代码库上有效协作，扩展了审查视角。
---

## 摘要
现有的多智能体软件开发系统提出了多种智能体协作形式，包括基于角色的协作和自动化代码审查。然而，许多系统假设存在共同的运行时、中央对话服务器或同一API系列。在这些假设下，来自不同供应商的LLM智能体无法在共享代码库上进行开发和审查工作的同时，轻易地从各自的执行环境直接交换消息。本文提出了tap，一种基于文件的协作协议，允许Claude（Anthropic）和Codex（OpenAI）在没有共享内存或相同运行时的情况下，在一个代码库上进行协作。tap的核心是一种文件优先的设计，它将带有元数据的markdown文件保留为原始消息，结合了文件检查路径（文件通信，第一层）与Claude和Codex的实时通知路径（实时通信，第二层），并通过单独的git工作树隔离工作。即使实时通知失败或接收方重启，消息文件仍然可用，并且可以再次检查相同的内容。在为期27天、37代的自应用操作中，tap被用于开发和审查自身，我们收集了209个与tap相关的拉取请求和717个操作工件。对375个审查工件的分析显示，记录至少一个缺陷或请求更改的审查比例在异构模型对中为69.8%，在同构模型对中为53.1%。这些结果表明，结合基于文件的消息保留和实时通知的tap能够在真实的生产仓库中运行，并且结合异构模型和执行环境可以拓宽审查视角。tap以开源npm包@hua-labs/tap（v0.5.2）的形式发布。

## Abstract
Existing multi-agent software development systems have proposed many forms of agent collaboration, including role-based collaboration and automated code review. However, many systems assume a common runtime, a central conversation server, or the same API family. Under these assumptions, LLM agents from different vendors cannot easily exchange messages directly from their own execution environments while dividing development and review work on a shared codebase. This paper presents tap, a file-based collaboration protocol that allows Claude (Anthropic) and Codex (OpenAI) to collaborate on one codebase without shared memory or an identical runtime. The core of tap is a file-first design that preserves markdown files with metadata as original messages, combines a file inspection path (file communication, Tier 1) with real-time notification paths for Claude and Codex (real-time communication, Tier 2), and isolates work through separate git worktrees. Even if real-time notification fails or a receiver restarts, the message file remains available and the same content can be inspected again. In a 27-day, 37-generation self-applied operation where tap was used to develop and review itself, we collected 209 tap-related pull requests and 717 operational artifacts. An analysis of 375 review artifacts showed that the share of reviews recording at least one defect or requested change was 69.8% for heterogeneous model pairs and 53.1% for homogeneous model pairs. These results show that tap, which combines file-based message preservation with real-time notification, operates in a real production repository, and that combining heterogeneous models and execution environments can broaden review perspectives. tap is distributed as the open-source npm package @hua-labs/tap (v0.5.2).

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：现有多智能体系统（如 ChatDev、MetaGPT、AutoGen）假设所有智能体运行在相同的模型家族、共同的运行时、中央对话服务器或同一 API 家族上。这种设计使得来自不同厂商（如 Anthropic 与 OpenAI）的 LLM 智能体难以在各自独立执行环境中直接交换消息，并在共享代码库上协作开发与审查。
- **核心问题**：如何在不依赖共享内存、相同运行时或中央协调服务器的情况下，让异构 LLM 智能体（如 Claude 和 Codex）在同一代码库上实现高效、可靠的消息交换与分工协作。
- **整体含义**：提出一种轻量级、基于文件的协议 tap，通过文件系统作为消息的权威存储介质，结合各执行环境特有的实时通知路径，打破模型供应商和运行时的壁垒，实现跨厂商、跨执行环境的智能体协作。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：**文件优先（File-first）设计**——消息首先以带 YAML 元数据的 Markdown 文件写入 `inbox/` 目录，此文件即为原始消息（权威记录）；之后根据各智能体执行环境采用不同的传递路径。
- **两层通信路径**：
  - **Tier 1（文件通信）**：通过 MCP 工具直接读取 `inbox/` 中的消息文件。这是始终可用的基础路径，即使实时通知失败，接收方仍可通过文件检查恢复消息。
  - **Tier 2（实时通信）**：针对 Claude 使用 MCP 通道通知，针对 Codex 使用 WebSocket（通过 Bridge 传送程序）。此路径降低对话延迟，但接收方仍以文件内容为准，不依赖通知负载。
- **配置管理**：`tap add` 命令自动检测当前执行环境（Claude 的 `.mcp.json`、Codex 的 `config.toml`、Gemini 的 JSON 配置），按相同流程生成变更计划并应用，不强制所有智能体使用统一 API。
- **工作空间隔离**：每个智能体运行在独立的 Git 工作树（git worktree）上，避免文件冲突；同时通过 `instanceId` 区分 PID 文件、日志和状态文件，支持同类智能体并行。
- **失败恢复**：发送方先完成文件写入再触发实时通知；接收方收到通知后重新读取文件原文。因此 WebSocket 断开、MCP 通知丢失等仅造成传递延迟，不会导致消息丢失。后续文件检查路径可发现未处理文件并重新处理。

## 3. 实验设计

- **数据集/场景**：**自应用实验**——使用 tap 协议自身作为待开发软件，智能体团队（Claude 和 Codex）在 27 天内历经 37 代（generation，因上下文限制或任务完成而更换团队），对 tap 本身进行开发、审查和维护。真实生产仓库包含多个应用和包。
- **Benchmark**：无外部基准数据集，而是通过收集操作工件（pull request、审查记录等）进行分析。主要对比了**异构模型对**（Claude 审查 Codex 工作、Codex 审查 Claude 工作）与**同构模型对**（Claude 审查 Claude、Codex 审查 Codex）在缺陷检测率上的差异。
- **对比方法**：没有直接与其他多智能体系统（如 A2A）做定量对比，但文中提及 A2A 和 MCP 协议，并说明 tap 关注的是本地仓库级别的基于文件的协作，与 A2A 的 HTTP/SSE 网络协作问题不同。定性讨论了与 ChatDev、MetaGPT、AutoGen 等的差异。

## 4. 资源与算力

- **文中未明确说明**：论文未提及使用的 GPU 型号、数量、训练时长等具体算力信息。仅说明使用了 Claude（Anthropic）和 Codex（OpenAI）的商业服务，以及 Gemini 的实验性参与，属于调用外部 API 而非本地训练。运行环境为多台服务器（Codex 审查者在独立的 Linux 服务器上以无头模式持续运行）。因此无法量化算力开销。

## 5. 实验数量与充分性

- **实验数量**：
  - 操作周期：27 天，37 代智能体团队。
  - 产出：209 个合并的 tap 相关 PR，717 个操作工件。
  - 分析样本：375 个审查和再审查工件（review artifacts）。
- **实验充分性与客观性**：
  - **优点**：长时间的真实运行（非一次性实验），积累了大量实际操作记录，覆盖了失败恢复、协议演进等场景。分析层面区分了异构/同构模型对。
  - **不足**：属于**观察性研究**，而非受控实验。审查者分配非随机（后期大量 Codex 审查 Claude 工作），时间效应和配对效应混淆。同一个 PR 可能被多次审查并重复计数。仅在一个组织上下文（单个仓库、单一操作惯例）中执行，外部有效性有限。实时协作仅涉及 Claude 和 Codex 两个模型，Gemini 仅实验性参与。tap 自身在 37 代中不断演进，早期和晚期操作条件不一致，LLM 基础模型和能力也可能随时间变化。因此，实验虽充分展示了可行性，但结论在泛化时需要谨慎。

## 6. 主要结论与发现

1. **协议可行性**：tap 成功使 Claude 和 Codex 在无中央协调服务器的情况下，通过文件系统和实时通知在同一个仓库中持续协作 27 天。
2. **异构协作的缺陷检测优势**：在 375 个审查工件中，异构模型对（Claude+Codex）的缺陷检测率为 **69.8%**（183/262），同构模型对仅为 **53.1%**（60/113）。提示不同模型和执行环境能拓宽审查视角。
3. **文件即外部记忆**：717 个操作工件（回顾、发现、审查、交接）作为文件持久化，为后续代际智能体提供了工作上下文和决策依据，弥补了智能体无持久内部状态的不足。
4. **协议自演化**：操作中发现的失败（如路径前缀、日志零字节、名称冲突）被反馈为协议改进（见表2），体现了自应用环境的迭代优化能力。

## 7. 优点

- **设计理念新颖**：文件优先设计兼顾消息的权威性（持久化、可审计）和低延迟（实时通知），且不依赖特定厂商的运行时或对话服务器。
- **实用性强**：以真实生产仓库的自应用操作验证了协议的鲁棒性和有效性，强调了跨执行环境协作的实际价值。
- **成本可解释**：通过 Git 工作树隔离和配置自动化，降低了异构智能体集成的工程难度。
- **分析有洞察**：尽管存在混淆因素，对异构/同构配对缺陷检测率的统计对比直观表明多样性有助于提高审查质量。

## 8. 不足与局限

- **实验控制不足**：数据为观察性，非随机分配；PR 多次审查重复计数；无法分离模型类型、角色、跨操作系统等影响。
- **外部有效性局限**：单一组织、单个仓库、特定操作实践；仅支持 Claude 和 Codex 实时通信，Gemini 仅文件检查。
- **协议变化影响**：tap 自身在 37 代中不断调整，导致前后操作条件不同。
- **LLM 变动风险**：基础模型升级、工具能力变化可能影响结果的可复现性。
- **安全分析粗糙**：仅识别 12 个安全相关 PR，但未按模型配对做定量分离，仅为定性证据。
- **未提供对比基准**：未与其他多智能体协议（如 AutoGen、A2A）在同一任务上进行正面性能比较。

（完）
