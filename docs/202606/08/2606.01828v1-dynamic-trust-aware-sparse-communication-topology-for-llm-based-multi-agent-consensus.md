---
title: Dynamic Trust-Aware Sparse Communication Topology for LLM-Based Multi-Agent Consensus
title_zh: 基于LLM的多智能体共识的动态信任感知稀疏通信拓扑
authors: "Wanshuang Gou, Zihan Liu"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01828v1"
tags: ["query:eca"]
score: 8.0
evidence: 面向LLM多代理共识的动态稀疏通信拓扑
tldr: 现有LLM多智能体系统采用全连接通信，导致消息数和Token成本随智能体数量平方增长，而固定稀疏拓扑无法适应不同任务。为此，本文提出DySCo动态信任感知稀疏共识机制，每轮基于可靠性、答案分歧和任务相关性估计通信边价值，在预算约束下选择高价值边交换，并通过动态信任权重聚合答案及早期终止策略。在数学推理、逻辑推理和事实问答任务上，DySCo在保持或提升准确率的同时显著降低通信开销和延迟。该方法实现了自适应稀疏通信，兼顾了效率与跨验证信息的保留。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01828v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 868, \"height\": 385, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01828v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1778, \"height\": 953, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01828v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 871, \"height\": 350, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01828v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 879, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01828v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 868, \"height\": 249, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01828v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1840, \"height\": 398, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01828v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 883, \"height\": 220, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01828v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 955, \"height\": 780, \"label\": \"Table\"}]"
motivation: 全连接通信开销随智能体数平方增长，固定稀疏拓扑无法适应任务变化，导致低效或丢失关键纠错信息。
method: 提出DySCo机制，每轮基于信任度、答案分歧和任务相关性选择高价值边通信，动态权重聚合并早期终止，实现自适应稀疏拓扑。
result: 在数学推理、逻辑推理和事实问答任务上，DySCo在保持准确率的同时显著降低Token成本和延迟。
conclusion: DySCo实现了高效通信与共识质量的平衡，为大规模多智能体协作提供了可扩展方案。
---

## 摘要
大型语言模型驱动的多智能体系统通过多轮讨论、角色专业化和交叉验证来增强复杂推理任务的可靠性。然而，现有的多智能体辩论与协作框架通常采用全连接通信，导致消息数量、token成本及端到端延迟随着智能体数量近似二次增长；尽管固定稀疏拓扑降低了开销，但它们无法根据不同的任务实例或中间推理状态调整通信关系，容易要么保留低价值交互，要么丢失关键的纠错信息。针对这一问题，本文提出DySCo（动态稀疏共识），一种动态信任感知稀疏共识机制。在每一轮推理中，DySCo基于智能体可靠性、答案分歧和任务相关性估计通信边的价值，并在预算约束下选择少量高价值边进行消息交换；随后通过动态信任权重聚合不同智能体的答案，并在共识稳定后提前终止讨论。该机制以按需通信替代全局广播，从而在保留必要交叉验证信息的同时降低通信开销。我们进一步给出了通信复杂度与共识稳定性的分析，并在数学推理、逻辑推理和事实问答任务上评估了DySCo的性能。

## Abstract
Large language model-driven multi-agent systems enhance the reliability of complex reasoning tasks through multi-round deliberation, role specialization, and cross-validation. However, existing multi-agent debate and collaboration frameworks typically adopt fully connected communication, causing the number of messages, token costs, and end-to-end latency to grow approximately quadratically with the number of agents; although fixed sparse topologies reduce overhead, they cannot adapt communication relationships to different task instances or intermediate reasoning states, making them prone either to preserving low-value interactions or to losing critical error-correction information. To address this problem, this paper proposes DySCo (Dynamic Sparse Consensus), a dynamic trust-aware sparse consensus mechanism. In each round of reasoning, DySCo estimates the value of communication edges based on agent reliability, answer divergence, and task relevance, and selects a small number of high-value edges for message exchange under budget constraints; it then aggregates the answers of different agents through dynamic trust weights and terminates the discussion early once consensus stabilizes. This mechanism replaces universal broadcasting with on-demand communication, thereby reducing communication overhead while preserving essential cross-validation information. We further present analyses of communication complexity and consensus stability, and evaluate the performance of DySCo on mathematical reasoning, logical reasoning, and factual question-answering tasks.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **研究动机**：现有基于LLM的多智能体系统（如多智能体辩论MAD、ChatDev、AutoGen等）通常采用全连接通信，导致消息数量、Token成本和端到端延迟随智能体数量呈近似二次增长（O(Rn²)）。尽管已有工作引入固定稀疏拓扑（如环图、随机图）来降低开销，但这些固定结构无法根据任务实例或中间推理状态动态调整通信关系，容易保留低价值交互或丢失关键纠错信息。
- **整体含义**：本文提出一种动态信任感知稀疏共识机制DySCo，旨在用按需通信替代全局广播，在保持必要交叉验证信息的同时显著降低通信开销，实现效率与共识质量的平衡。

## 2. 方法论

### 核心思想
- 每轮推理中，DySCo基于智能体历史可靠性、当前置信度、答案分歧程度、任务相关性以及通信成本，动态评估每条潜在通信边的价值，在预算约束下为每个接收者选择Top-k个高价值发送者。
- 信任加权聚合：接收者根据邻居的信任权重处理反馈，更新自身状态；系统通过信任加权投票形成最终共识。
- 早期停止：当加权答案分布的熵低于阈值或连续两轮共识不变且平均置信度增幅较小时，提前终止讨论。

### 关键技术细节
1. **智能体状态表示**：
   - 每个智能体在时刻t维护 `(y_t, r_t, c_t)`，分别表示当前答案、推理过程和置信度。
2. **动态边选择**：
   - 对于每条有向边(j→i)，计算得分：
     `s_{tij} = α·T_tj + β·c_tj + γ·D_{tij} + δ·H_{ij}(x) - η·L_tj`
     - T_tj：历史信任权重
     - c_tj：当前置信度
     - D_{tij}：答案分歧度（如是否不同选项）
     - H_{ij}(x)：任务依赖/角色互补性
     - L_tj：预计Token成本
     - α,β,γ,δ,η为超参数。
   - 每个接收者选择得分最高的k个发送者，每轮最多 `nk` 条消息。
3. **消息压缩与批判生成**：
   - 发送者生成结构化压缩消息：包含当前答案、最关键的支撑理由、一个可能的反例或修正建议（不超过120词）。
4. **信任感知状态更新**：
   - 接收者提示区分“保留原答案”“部分修改”“完全改变”三种情况，并要求按信任权重处理不同邻居的意见。
   - 信任权重更新：`T_{t+1,i} = λ·T_ti + (1-λ)(ρ·V_ti + (1-ρ)·A_ti)`，其中V_ti为轻量验证者评分，A_ti为与临时多数共识的一致性。
5. **共识聚合与早期停止**：
   - 最终答案：`ˆy = argmax_y Σ_i T_Ri · c_Ri · I[ y_Ri == y]`。
   - 当加权熵<ϵ时停止。

### 复杂度分析
- 全连接：O(Rn²·ℓ̄)
- DySCo：O(Rn·k·ℓ̄_c)，其中k≪n且ℓ̄_c≤ℓ̄，大幅降低。

## 3. 实验设计

### 数据集与场景
- **数学推理**：GSM8K
- **逻辑与常识推理**：LogiQA、StrategyQA
- 每个数据集随机采样300个实例。

### Benchmark与对比方法
- 基线方法：
  - 单智能体Chain-of-Thought (Single-Agent CoT)
  - Self-Consistency（n=6采样聚合）
  - 全连接多智能体辩论（Dense MAD）
  - 固定环拓扑（Static Ring）
  - 随机选择k个邻居（Random-k）
  - 仅基于历史信任的稀疏拓扑（Trust-Only Sparse）
- 评估指标：Accuracy、平均Token消耗（千）、端到端延迟（秒）、共识熵。

### 实验设置
- 基础模型：gpt-3.5-turbo
- 智能体数量：默认n=6（可扩展性实验取4/8/12）
- 最大通信轮数R=3
- 邻居预算k=2（可扩展实验中k=⌊n/3⌋）
- 超参数：默认α=0.30, β=0.20, γ=0.25, δ=0.15, η=0.10, ϵ=0.30, λ=0.8, ρ=0.5

## 4. 资源与算力
- 论文未明确说明GPU型号、数量及训练时长。实验基于gpt-3.5-turbo API调用，未涉及模型训练，仅进行推理和API调用。因此算力消耗主要体现在API成本上，未提供具体数值。

## 5. 实验数量与充分性

### 实验组数
- **主实验**（表1）：3个数据集 × 7种方法 = 21组对比结果，每个结果取3个随机种子的均值和标准差。
- **消融实验**（表2）：GSM8K上6种变体（去除信任权重、去除多样性分数、去除任务依赖、去除消息压缩、去除早期停止），每个变体与完整模型对比。
- **可扩展性实验**（表3）：GSM8K上智能体数量4/6/8/12，对比Dense MAD与DySCo。
- **共识动态分析**（表4）：LogiQA上追踪t=0~3轮的熵和精度。
- **超参数敏感性分析**（表5）：对k, R, ϵ, α, β, γ, δ, η各取2~3个值，共约20组设置。

### 充分性与公平性
- **充分性**：覆盖了不同任务类型、多个基线（包括单智能体、全连接、多种固定稀疏拓扑）、消融关键组件、可扩展性、共识动态和超参数敏感性，实验设计较为全面。
- **公平性**：所有方法使用相同基础模型（gpt-3.5-turbo），提示模板固定基线字段，随机种子多次重复报告均值和标准差。但未提及是否对基线进行超参数调优以公平比较（例如固定拓扑是否也进行了类似的消息压缩？需注意文中消息压缩是DySCo特有组件，消融实验显示去掉压缩导致准确率略升但Token剧增，这意味着在公平比较时基线可能没有使用等价的压缩策略，因此Token对比可能不完全对等）。

## 6. 主要结论与发现

1. **性能**：DySCo在三个数据集上均取得最高准确率（GSM8K 84.3%、LogiQA 78.5%、StrategyQA 85.4%），显著优于全连接MAD（82.0%/76.8%/83.5%）。
2. **效率**：Token消耗比Dense MAD降低约70%（从14.8k降至4.5k），延迟降低近一半（18.6s降至9.8s）。
3. **可扩展性**：随着智能体数量从4增至12，Dense MAD的Token成本平方增长至58.4k且准确率在n>8后停滞甚至下降；DySCo的Token成本线性增长至10.5k，准确率持续提升至86.8%。
4. **共识收敛**：DySCo在第一轮通信后熵从1.54降至0.62（Dense MAD为0.88），更快达成共识，且最终精度更高。
5. **消融证实**：信任权重和多样性分数对精度贡献最大；消息压缩在几乎不影响精度的前提下大幅节省Token；早期停止减少无效轮次。
6. **超参数鲁棒性**：不同设置下性能稳定，k=2为最佳平衡点，R=3已足够。

## 7. 优点

- **方法创新**：将通信拓扑从固定结构扩展为动态决策变量，基于多维度信号（信任、置信度、分歧、任务相关性、成本）进行自适应稀疏化，比现有固定稀疏拓扑更灵活有效。
- **理论支撑**：给出了通信复杂度分析（O(Rnk)），以及基于时变随机矩阵的弱共识条件分析，增强了方法的可信度。
- **实践价值**：显著降低Token成本和延迟，使得多智能体系统在大规模部署时更具经济性和响应速度。
- **实验全面**：涵盖多种任务、多个消融、可扩展性和超参数敏感性，结果统计显著（报告标准差）。
- **可复现**：提供了完整的提示模板（Solve、Critique、Revise、Consensus Normalization），便于复现。

## 8. 不足与局限

- **置信度校准风险**：LLM自报置信度可能不准确，直接使用可能放大过度自信的错误。论文提及但未提供实际校准策略。
- **信任权重的“富者愈富”效应**：早期正确的智能体可能获得过高信任，导致后续通信集中在少数节点，可能抑制多样性。论文建议引入ϵ-贪婪或置信上界探索，但未在实验中实现。
- **局部共识风险**：过于稀疏的通信可能导致形成局部群簇而无法达成全局共识。论文建议周期性地引入随机跨簇边，但未验证效果。
- **领域限制**：该方法主要适用于可验证的任务（数学、逻辑、事实问答），对于开放式生成、价值判断等任务（如创意写作、伦理判断）的效果未验证。在这些任务中，分歧可能只反映风格差异而非信息互补。
- **基线对比公平性**：DySCo使用了消息压缩和信任加权，而固定稀疏拓扑基线（Static Ring等）可能未经相应优化，因此Token节省部分归功于压缩而非稀疏拓扑本身。消融显示去掉压缩后Token暴涨，但精度略升，说明压缩会轻微损失信息。基线若采用同样压缩可能部分缩小差距。
- **非LLM线性模型更新**：共识稳定性分析仅给出抽象解释，实际LLM更新并非线性系统，因此理论保证有限，最终依赖实证。
- **计算开销**：每轮计算边缘得分需要额外的LLM推理（如判断分歧、任务依赖），这部分成本未在Token统计中显式体现，可能导致实际总成本被低估。
- **实验规模**：每个数据集仅300个样本，且仅使用gpt-3.5-turbo一种模型，未在更大模型（如GPT-4）或其他开源模型上验证泛化性。

（完）
