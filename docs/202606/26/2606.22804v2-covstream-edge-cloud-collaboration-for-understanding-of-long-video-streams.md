---
title: "CoVStream: Edge-Cloud Collaboration for Understanding of Long Video Streams"
title_zh: CoVStream：面向长视频流理解的边缘-云协作
authors: "Xu Liu, Guikun Chen, Zihao Yan, Kanzhi Wu, Wenguan Wang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22804v2"
tags: ["query:eca"]
score: 9.0
evidence: 边缘云协作框架
tldr: "长视频流理解受限于边缘设备计算能力和云带宽，现有方法需在云端强推理和边缘低带宽间取舍。CoVStream通过边缘提取紧凑视觉特征和语义描述传输至云端，云端构建实体图与全局上下文，仅在用户查询时激活大模型推理。在VideoMME-Long、LVBench等基准上，带宽降低87.6%同时保持99.2%云基线精度。该框架首次实现边缘-云高效协作，为长视频流处理提供新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22804v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 874, \"height\": 630, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22804v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1815, \"height\": 685, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22804v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1829, \"height\": 552, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.22804v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1634, \"height\": 710, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22804v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1815, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22804v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 910, \"height\": 389, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22804v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 842, \"height\": 208, \"label\": \"Table\"}]"
motivation: 当前长视频流理解忽视边缘设备计算限制，云卸载带宽高，边缘处理性能差，需高效协作方案。
method: 边缘节点蒸馏视频为紧凑视觉特征和语义描述传云；云端整合为实体图和全局上下文，按需激活重推理模型。
result: "在LVBench上带宽降低87.6%，精度保留99.2%云基线，优于现有方法。"
conclusion: CoVStream是首个边缘-云协作框架，平衡带宽与精度，实现长视频流高效理解。
---

## 摘要
长连续视频流是多媒体智能日益关键的驱动力。现有工作通常采用“采样-编码-推理”的方式，利用大模型处理长视频。然而，它们忽略了一个关键的部署事实：视频流通常由计算受限的设备生成。这导致了一个难以维持的妥协：云端卸载虽然带来了强大的推理能力，但造成了过高的带宽开销；而设备端处理则受限于边缘硬件能力。因此，我们提出CoVStream，首个面向长视频流理解的边缘-云协作框架。边缘节点将原始视频流提炼为紧凑的视觉特征和语义描述，传输至云端以最小化带宽成本；云端服务器将这些数据整合到实体图和全局视觉上下文中，仅在用户查询到达时激活重型推理模型。在VideoMME-Long、LVBench和RTV-Bench上的实验表明，CoVStream在保留LVBench上99.2%云端基线准确率的同时，将带宽使用量减少了87.6%。

## Abstract
Long, continuous video streams are an increasingly critical driver of multimedia intelligence. Existing efforts often handle long videos with a sample-encode-reason approach using large models. However, they overlook a crucial deployment fact: the stream is often produced by computationally constrained devices. This forces an untenable compromise: cloud offloading unlocks strong reasoning but incurs prohibitive bandwidth overhead, while on-device processing remains limited by edge hardware capacity. Therefore, we propose CoVStream, the first edge-cloud collaborative framework for understanding long video streams. The edge node distills raw video streams into compact visual features and semantic captions for transmission to the cloud, minimizing bandwidth costs, while the cloud server integrates this data into an entity graph and global visual context, activating the heavy reasoning model only when a user query arrives. Experiments on VideoMME-Long, LVBench, and RTV-Bench show that CoVStream reduces bandwidth usage by 87.6% while retaining 99.2% of the cloud baseline accuracy on LVBench.

---

## 论文详细总结（自动生成）

好的，遵照您的要求，以下是对论文《CoVStream: Edge-Cloud Collaboration for Understanding of Long Video Streams》的详细中文总结。

### 论文核心问题与整体含义

- **研究动机与背景**：长视频流理解（如AI眼镜的连续拍摄）是多媒体智能的关键。现有方法（如VideoLLaMA、TimeChat）通常采用“采样-编码-推理”并在云端部署大模型，虽然性能强大，但忽略了视频流通常来自**计算受限的边缘设备**（如智能眼镜）这一关键事实。
- **核心矛盾**：这导致了一个两难困境：
    - **纯云端方案**：推理能力强，但需持续传输原始视频，导致带宽成本过高。
    - **纯边缘方案**：节省带宽，但受限于硬件能力，无法运行复杂模型，性能差。
- **研究目标**：设计一个**边缘-云协作框架**，在边缘端进行高效压缩，在云端进行强大的按需推理，从而在**极低带宽成本下接近云端推理精度**。

### 方法论：CoVStream框架

- **核心思想**：利用视频流中的时空冗余，将边缘设备作为“语义压缩器”，将云端作为“长期记忆库和按需推理引擎”。云端仅在用户发出查询时才激活大模型，实现高效协作。
- **Edge Node（边缘节点）双流感知与压缩管道**：
    1.  **视觉特征提取**：对每帧 `v_t` 提取视觉特征 `f_t`。
    2.  **时序特征融合**：将多帧的特征通过迭代合并（递归计算余弦相似度合并最相似对）来压缩，得到紧凑的视觉特征 `Z_cond`。例如，将16帧特征合并为2帧，实现87.6%的压缩率。
    3.  **自适应关键帧选择**：并行地对特征进行聚类，选出最接近聚类中心的帧作为关键帧 `K`。
    4.  **语义描述生成**：对关键帧，使用轻量级语言模型（如Qwen3-VL-2B）生成简单句描述 `C_text`。
    5.  **最终传输**：边缘上传的语义载荷 `Pload = {Z_cond, C_text}`。
- **Cloud Server（云端服务器）解耦管理与推理架构**：
    1.  **内存管理（持续运行）**：
        - **全局视觉上下文维护**：接收 `Z_cond` 并存入一个容量有限的压缩库，防止无限增长。
        - **实时实体图构建**：将文本描述 `C_text` 解析为“主-谓-宾”三元组，构建一个动态的实体图 `G`。新实体与30秒窗口内的候选实体通过自然语言推理（NLI）进行融合，避免冗余。
    2.  **按需推理（事件驱动）**：
        - **默认休眠**：推理模型默认处于睡眠状态，节省资源。
        - **触发激活**：收到用户查询 `Q` 后，模型被唤醒。
        - **图增强推理**：
            - 用户查询向量化。
            - 在实体图中检索出Top-K个最相关的描述。
            - 以这些描述对应的节点为中心，提取其1跳邻居子图。
            - 将子图和全局视觉上下文序列化，连同原始查询一起输入大模型，生成最终回答。

### 实验设计

- **数据集**：在三个流媒体基准上评估：
    - **VideoMME-Long**: 长视频，含900个选择题，平均时长3160秒。
    - **LVBench**: 超长视频，含1007个问题，平均时长5018秒。
    - **RTV-Bench**: 实时流视频，共167.2小时，平均时长1092秒。
- **Benchmark对比方法**：
    - **Local-Only (LO)**：边缘端直接使用轻量模型（VideoLLaMA3-2B）推理，性能下界。
    - **Cloud-Only (CO)**：原始视频上传至云端，使用大模型（VideoLLaMA3-7B）推理，性能上界。
    - **Edge-Cloud (EC)**：边缘端采样并降采样后上传，云端超分辨率恢复后进行推理。

### 资源与算力

- **主要实验平台**：NVIDIA A40 (48GB) GPU。
- **边缘模型**：0.4B参数量的视觉编码器 + 2B参数量的描述生成模型。
- **云端模型**：7B参数量的VideoLLaMA3。
- **算力分析**：整个框架是**训练无关**的，即不需要额外的模型训练。所有模型均为预训练或开源模型。因此，论文未提及训练时长。仅需在边缘端和云端进行推理。论文提到在边缘端，总计算需求为 **5.9 TFLOPs**（在密集BF16精度下），且能在RTX 2080 (8GB) 上稳定运行。

### 实验数量与充分性

- **实验数量**：
    - **主结果**：在3个基准上对比了4种方法（LO、CO、EC、CoVStream），报告了准确率、带宽、延迟。
    - **消融实验**：对三个核心组件（T: 时序特征融合、S: 自适应关键帧选择、G: 实体图构建）进行了7种组合的消融研究。
    - **诊断实验**：
        - 压缩策略权衡（窗口尺寸 vs. 输出帧数）。
        - 时间鲁棒性（按视频时长分段分析）。
        - 内存可扩展性（模拟24小时连续流，监测内存占用）。
        - 硬件部署压力测试（A40, RTX 3090, RTX 2080）。
- **充分性评估**：
    - **非常充分**：实验设计全面，覆盖了性能、带宽、延迟、内存、鲁棒性、硬件适配等多个维度。消融实验清晰地证明了每个组件的贡献。
    - **客观公平**：与多个baseline在多个基准上对比，结果清晰。然而，所有实验均基于同一种推理模型（VideoLLaMA3），可能存在选择偏差。
    - **客观性**：实验设计客观，结论有数据支撑。

### 主要结论与发现

- CoVStream成功平衡了**性能、带宽和延迟**：
    - 在LVBench上，带宽降低**87.6%**，准确率保留**99.2%** 的云端基线。
    - 端到端延迟（2.99s）优于云端方案（4.08s），接近边缘方案（2.72s）。
- **超长视频鲁棒性**：在超过6000秒的视频上，CoVStream准确率（42.92%）**反超**并**领先**纯云端方案（35.00%）7.92%。这说明结构化实体图在处理极长序列时，相较于大模型受限于上下文窗口的优势。
- **内存可扩展性**：在模拟24小时连续流中，CoVStream的云端内存占用稳定在**438.3 MB**，而对比方法则线性增长至**229.5 GB**，实现了**523倍**的减少。这证明了其“始终在线”部署的可行性。

### 优点

1.  **训练免费（Training-Free）**：无需额外训练，可直接与现有大模型集成，即插即用。
2.  **高效压缩**：通过双流管道（特征融合+关键帧选择）实现极高的带宽节省。
3.  **异步解耦**：云端的内存管理与推理过程异步，确保了低延迟响应。
4.  **结构化记忆**：实体图提供了比原始视频帧更高效、更鲁棒的记忆形式，尤其适合超长视频。
5.  **实验全面**：设计周密的诊断实验，特别是对超长视频和24小时模拟场景的评估，极具说服力。

### 不足与局限

1.  **实验基准偏差**：所有实验均使用VideoLLaMA3作为推理模型，其在不同模型上的泛化能力未经验证。
2.  **依赖文本质量**：实体图的构建和推理完全依赖于边缘端生成的文本描述质量。如果描述生成模型出错或内容稀疏，会导致图结构不准确，进而影响推理结果。
3.  **硬件假设**：虽然进行了压力测试，但计算节点（0.4B + 2B模型）仍需要一定的GPU能力，在极低功耗设备（如智能眼镜SoC）上的实际表现未验证，其提到的“5.9 TFLOPs”需求依然不低。
4.  **图结构局限性**：当前图主要基于“主-谓-宾”结构，对更复杂的时空关系（如因果关系、事件顺序）建模能力有限。
5.  **内存增长问题**：虽然总内存稳定，但实体图部分仍在持续缓慢增长（122.3 MB/24h），长期运行下仍需关注。

（完）
