---
title: Joint Communication and Computation Design for Mobile Embodied AI Network (MEAN)
title_zh: 移动具身人工智能网络（MEAN）的联合通信与计算设计
authors: "Chenliang Wu, Zhouxiang Zhao, Jiaxiang Wang, Ruopeng Xu, Chen Zhu, Zhaohui Yang, Zhaoyang Zhang"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14300v1"
tags: ["query:eca"]
score: 9.0
evidence: 移动具身人工智能网络，具有协作策略和基站辅助
tldr: 针对移动具身AI网络中代理协作任务的能耗优化问题，本文联合考虑通信、计算与任务执行能耗及协作增益，建立混合整数非线性规划模型。提出低复杂度枚举算法，通过凸性推导获得语义压缩比和发射功率的闭式最优解，并基于个体节能潜力用贪心排序确定协作模式与规模。仿真显示算法显著降低总能耗，为MEAN的节能协作提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14300v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1844, \"height\": 701, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.14300v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1860, \"height\": 530, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.14300v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 737, \"height\": 386, \"label\": \"Table\"}]"
motivation: 降低移动具身AI网络(MEAN)中协作任务的能耗，需联合优化通信、计算与任务执行。
method: 能耗优化建模为MINLP，通过凸性推导语义压缩比与发射功率闭式解，基于节能潜力贪心排序确定协作模式。
result: 仿真表明所提算法相比基准方案显著减少系统总能耗。
conclusion: 为MEAN的节能协作提供了有效的联合通信与计算设计框架。
---

## 摘要
本文研究了移动具身人工智能网络（MEAN）在无线通信上的能效协作策略问题。在所考虑的模型中，智能体通过协作执行任务，并且可以根据信噪比（SNR）和全局协作在两种运行模式之间切换。双模式包括基站（BS）辅助协作模式（智能体通过语义通信与基站交互做出决策，然后协作完成任务）和本地计算模式（智能体独立决策并执行任务）。由于动态无线通信和灵活协作策略，我们联合考虑计算能耗、通信能耗以及具有特定协作增益的任务执行能耗，将其转化为一个混合整数非线性规划（MINLP）优化问题，目标是最小化系统总能耗。为解决该问题，我们提出了一种低复杂度的枚举算法：首先，通过证明严格凸性，得到语义压缩比和发射功率的最优闭式解。其次，基于个体节能潜力，采用贪心排序算法确定每个智能体的协作规模和运行模式。仿真结果表明，与基准方案相比，所提算法能显著降低总能耗。

## Abstract
This letter investigates the problem of energy efficient collaborative strategy for mobile embodied artificial intelligence network (MEAN) over wireless communication. In the considered model, the agents execute the tasks through collaboration, and they can switch between two operating modes based on the signal-to-noise ratio (SNR) and global collaboration. The dual-mode comprises the base station (BS)-assisted collaborative mode, in which agents make decisions through semantic communication with BS and then collaborate on tasks, and the local computing mode, in which the agents make decisions and execute tasks independently. Due to the dynamic wireless communication and flexible collaboration strategy, we jointly consider computation energy, communication energy, and task-execution energy with specific collaborative gains into a mixed-integer nonlinear programming (MINLP) optimization problem whose goal is to minimize the total system energy consumption. To solve it, we propose a lower-complexity enumeration algorithm: first, we get the optimal closed-form solution for semantic compression ratio and transmit power by proving the strict convexity. Second, we determine the scale of collaboration and the operating mode of each agent by a greedy sorting algorithm based on individual energy-saving potentials. Simulation results show that the proposed algorithm can significantly reduce the total energy consumption compared to benchmark schemes.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：移动具身人工智能网络（MEAN）由多个具备自主分析和任务执行能力的智能体（如无人机、四足机器人等）组成。它们通过无线通信进行信息交换和任务分配，以更高效、鲁棒地解决复杂问题（如搜索、场景建模、装配等）。
- **核心问题**：现有控制框架大多假设理想通信条件，但在实际部署中，无线通信存在有限带宽、多径衰落和动态干扰等瓶颈，导致智能体间信息交换不可靠。同时，智能体能量有限，需高效处理计算、通信和任务执行三大能耗。如何在动态无线环境下设计能效最优的协作策略，是当前面临的主要挑战。
- **研究含义**：为应对上述挑战，本文提出将**语义通信**（仅传输任务相关特征而非原始数据）与**灵活协作策略**相结合，通过联合优化语义压缩比、发射功率和运行模式，最大化系统能效。该工作对于推动具身AI在多智能体系统中的实际部署具有重要意义。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：构建一个双模式运行策略（BS辅助协作模式 vs. 本地计算模式），智能体根据信道SNR和全局协作规模动态选择模式。目标是最小化系统总能耗（计算能耗 + 通信能耗 + 任务执行能耗）。
- **关键技术细节**：
  - **系统模型**：考虑一个基站（BS）和N个智能体。每个智能体在限定处理时间T0内完成感知数据处理与任务执行。定义二元变量xi表示模式（1为BS协作模式，0为本地计算模式）。只有SNR≥阈值的智能体（ai=1）才能选择协作模式。
  - **数据处理模型**：BS协作模式下，智能体对原始数据（大小Di）进行语义压缩（压缩比ρi），压缩时间与CPU频率、算法复杂度相关；然后通过无线链路传输，传输速率与功率pi相关。总处理时间需满足T0约束。本地模式直接本地处理。
  - **能耗模型**：
    - 信息处理能耗：BS模式包括语义压缩能耗 + 上行传输能耗；本地模式仅本地计算能耗。
    - 任务执行能耗：采用通用可扩展性定律（USL）描述协作增益函数G(K)，其中K为协作智能体数量。协作可降低每智能体任务执行能耗（基线Q乘以因子G(K)），但同时引入竞争开销（ξ参数）。
  - **优化问题**：混合整数非线性规划（MINLP），决策变量为压缩比ρ、发射功率p、模式选择x。约束包括处理时间、SNR可行性、压缩比范围、功率上限。
- **算法流程**（低复杂度枚举算法）：
  1. **连续变量优化**：对每个智能体（假设在BS模式下），证明目标函数（E_BS）关于ρ_i严格凸，通过求导并利用二分法得到最优压缩比ρ_i*的闭式解，再由功率-速率关系得到最优功率p_i*。
  2. **离散变量优化**：计算每个可行智能体的节能潜力ΔE_save* = E_local - E_BS*。将所有可行智能体按ΔE_save*降序排列；枚举可能的协作规模K（从2到M，M为SNR可行智能体总数），对每个K，选择前K个智能体进入协作模式，计算系统总能耗（包括协作增益函数Ψ(K)），取使总能耗最小的K*及对应模式分配。
- **算法复杂度**：O(M² log M + M² log(1/ε))，远低于传统MINLP的O(2^M)。

## 3. 实验设计：使用的数据集/场景、基准、对比方法

- **数据集/场景**：论文未使用真实数据集，而是采用仿真场景。仿真参数包括：带宽B=1 MHz，最大发射功率Pmax=1 W，噪声功率σ²=4×10^{-11} W，SNR阈值γth=1，时间约束T0=0.7 s，任务执行基线能耗Q=0.1 J，协作系数β=0.4，竞争系数ξ=0.008，数据大小D=10 Mbits（默认），智能体数量N=15（默认），CPU频率f=1 GHz，语义计算复杂度系数α=10，CPU每比特周期数τ=100，压缩比下限ρ_min=0.1，信道距离在50~1000 m随机生成。
- **基准（Benchmark）**：比较了四种基准方案：
  - **SNR-Based**：经典启发式，智能体仅在SNR满足阈值时选择BS协作模式。
  - **Local Only**：所有智能体均采用本地计算模式。
  - **No SemCom**：所有智能体采用BS协作模式但传输原始数据（无语义压缩）。
  - **Fixed Tx Power**：所有智能体发射功率固定为0.5 W（不优化）。
- **对比方法**：上述四种基准与所提算法（Proposed）进行性能对比，指标为总能耗。

## 4. 资源与算力

- **文中未明确说明**：论文未提及实验所用的GPU型号、数量、训练时长等算力资源。仅提到算法复杂度分析，仿真实现应为CPU平台上的数值模拟，不涉及大规模深度学习训练。

## 5. 实验数量与充分性

- **实验数量**：文中给出了三组主要实验，分别改变：
  - 智能体数量N（从2到20）
  - 数据大小D（从2到12 Mbits）
  - 时间约束T0（从0到1 s）
- **充分性评估**：实验覆盖了系统关键参数（代理规模、数据量、时间约束）对能耗的影响。但缺少以下方面：
  - 未进行消融实验（如单独验证语义压缩或协作策略的效果）。
  - 未对比不同SNR阈值或不同协作系数下的性能。
  - 未讨论不同任务执行能耗模型（如Q变化）的影响。
  - 未给出统计误差或多次随机种子下的平均结果（仅展示单次仿真曲线）。
  - 实验公平性尚可：所有基准在同一仿真参数下进行，对比有意义，但缺乏更复杂的基准（如基于强化学习的动态模式选择）。

## 6. 论文的主要结论与发现

- 所提算法在所有仿真场景下均优于四种基准方案，总能耗最低。
- 随着智能体数量增加、数据量增大或时间约束变紧，总能耗上升，但所提算法始终受益于语义压缩和最优协作策略，节能效果显著。
- 当时间约束放宽时，所提算法和SNR-Based方案能耗下降，因为允许更长的计算/通信时间以降低功率。
- 语义通信（No SemCom对比）和发射功率优化（Fixed Tx Power对比）均被证明能有效降低能耗，但联合优化效果最佳。

## 7. 优点

- **方法创新性**：首次在移动具身AI网络中联合优化语义压缩比、发射功率和运行模式，将语义通信与协作策略结合，解决动态无线环境下的能效问题。
- **算法高效性**：提出低复杂度枚举算法，利用凸性得到连续变量的闭式解，通过贪心排序降低了离散变量搜索复杂度，避免MINLP的指数爆炸。
- **理论严谨性**：严格证明了子问题的凸性，确保最优解唯一性；通过数学推导得到闭式表达式，算法可解释性强。
- **实用性**：考虑实际无线信道（SNR阈值）和协作开销（竞争系数），模型贴近真实部署。

## 8. 不足与局限

- **实验覆盖不足**：仅在一个仿真参数集下测试，未在不同信道模型、不同智能体异构性、不同任务类型下验证泛化能力。缺少消融实验独立量化各模块贡献。
- **假设简化**：假设信道在单个处理时间窗口内恒定，且所有智能体共享最大发射功率，未考虑多用户干扰或动态资源分配。
- **协作增益模型**：采用USL公式，参数β、ξ需预先设定，实际中可能难以准确获得。
- **未考虑移动性**：智能体移动可能导致信道随时间变化，本文模型为静态场景。
- **无真实设备验证**：所有结果基于仿真，缺乏在真实机器人平台上的实验支持。应用限制于理想仿真环境，实际部署需考虑延迟、同步、通信协议等工程问题。

---

（完）
