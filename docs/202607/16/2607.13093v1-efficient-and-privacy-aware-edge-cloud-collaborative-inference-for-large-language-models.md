---
title: Efficient and Privacy Aware Edge Cloud Collaborative Inference for Large Language Models
title_zh: 高效且隐私感知的边缘云协作推理用于大型语言模型
authors: "Yi Li, Chen Li, Jiexiong Liu"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.13093v1"
tags: ["query:eca"]
score: 8.0
evidence: 端云协同的LLM推理框架，兼顾隐私与效率
tldr: "设备端大模型推理面临延迟、资源与隐私的三重挑战。本文提出基于端点认证KV缓存的边缘云协同框架，本地处理嵌入与推测解码，云端执行认证推理与令牌验证，所有传输数据经量化加密。核心模块与草稿参数保留本地，支持CPU/GPU/嵌入式异构设备。实验表明每令牌延迟降低46.1%，下行负载减少67.4%，性能接近全云推理。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13093v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1501, \"height\": 918, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13093v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1589, \"height\": 377, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13093v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1608, \"height\": 459, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13093v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1710, \"height\": 414, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13093v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1711, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13093v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1710, \"height\": 465, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13093v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1716, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13093v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1711, \"height\": 306, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13093v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1715, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13093v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1714, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13093v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1710, \"height\": 325, \"label\": \"Table\"}]"
motivation: 全云推理暴露用户隐私，设备端推理受限于资源与延迟，亟需兼顾效率与隐私的协同方案。
method: 基于端点认证KV缓存的边缘云协同推理，本地负责嵌入、推测解码与低维计算，云端负责认证解码与令牌验证，数据量化加密传输。
result: "每令牌延迟降低46.1%，下行负载减少67.4%，性能接近全云推理。"
conclusion: 所提框架在隐私保护下实现了高效边缘云协同LLM推理，支持异构设备且性能优异。
---

## 摘要
设备端大语言模型推理面临响应延迟、硬件资源限制和用户隐私的三难困境。全云推理提供强大的计算能力但暴露用户提示和对话数据，而独立的设备端推理对大多数消费类和嵌入式边缘设备不可行。本文提出了一种基于端点认证KV缓存的隐私中心型边缘云协作LLM推理框架。本地端点处理输入预处理、嵌入计算、自适应特征优化、KV缓存认证、投机解码和低维模型头计算，而云则进行认证解码器推理、KV缓存管理、令牌验证和高维词汇投影。端点融合部分输出，应用语言自适应掩码并采样目标令牌。所有传输数据和截断的logits被量化和AES-GCM加密以保护隐私，核心轻量级模块、草稿参数和缓存访问策略保留在本地以避免泄露。该框架支持异构设备，包括仅CPU、配备GPU和嵌入式设备，通过优化流式传输、批处理和量化ONNX部署。评估表明，与基线分割推理相比，该框架将每个令牌的延迟降低高达46.1%，下行负载降低高达67.4%，同时保持与全云推理相当的性能。

## Abstract
On-device LLM inference faces a trilemma of response latency, limited hardware resources and user privacy. Full cloud inference delivers strong computing power but exposes user prompts and dialogue data, while standalone on-device inference is unfeasible for most consumer and embedded edge devices. This paper presents a privacy-centric edge-cloud collaborative LLM inference framework built on endpoint-authenticated KV cache. Local endpoints handle input preprocessing, embedding computation, adaptive feature optimization, KV cache authentication, speculative decoding and low-dimensional model head calculation, while the cloud conducts authenticated decoder inference, KV cache management, token verification and high-dimensional vocabulary projection. Endpoints fuse partial outputs, apply language-adaptive masking and sample target tokens. All transmitted data and truncated logits are quantized and AES-GCM encrypted for privacy, with core lightweight modules, draft parameters and cache access policies kept local to avoid leakage. The framework supports heterogeneous devices including CPU-only, GPU-equipped and embedded devices via optimized streaming, batching and quantized ONNX deployment. Evaluations demonstrate that the framework reduces per-token latency by up to 46.1\% and downlink payloads by up to 67.4\% over baseline split inference, retaining comparable performance to full cloud inference.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心矛盾**：大语言模型（LLM）在用户侧设备部署面临三重困境——交互延迟（低延迟要求）、有限本地计算资源（CPU/移动GPU/嵌入式设备）以及用户隐私暴露（输入提示和对话历史）。
- **现有方案不足**：
  - 纯云推理：计算充足，但暴露原始提示和长上下文标识符到远程服务，存在隐私风险。
  - 纯设备端推理：在消费级CPU、移动GPU和嵌入式设备上通常不可行（算力不足）。
  - 现有分割推理系统缺乏对KV缓存的细粒度控制、设备特定适配及对隐藏状态泄露的综合保护策略。
- **整体含义**：提出一种隐私感知的边缘-云协作推理框架，在保持效率的同时减少隐私暴露，支持异构设备部署。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将隐私敏感决策和轻量计算保留在端点，将重型解码器计算移至云端，并接受端点控制的缓存授权。
- 传输特征张量而非原始文本，使用量化+加密保护信道，端点本地保留LoRA模块、草稿参数和缓存访问决策。

### 关键技术细节
1. **端点认证KV缓存工作流**：
   - 端点生成嵌入、配置缓存使用（`flag_Cache`, `L_Cache`）、计算轻量本地投影、执行投机解码、融合云端返回的部分logits。
   - 云端接收特征张量，执行授权解码器推理，维护KV缓存，验证投机标记，计算高维词汇投影。
2. **分布式词汇投影（Split Vocabulary Projection）**：
   - 将LM-head权重沿隐藏维度分割：`W_lm = [W_end; W_cloud]`，`D_end + D_cloud = D`。
   - 端点计算`z_end = h_end * W_end`，云端计算`z_cloud = h_cloud * W_cloud + b`，端点相加融合。
   - 语言受限生成时，仅传输对应词汇子集`V_ℓ`的logits，减少下行负载。
3. **端点投机解码**：
   - 端点使用轻量草稿模块`g_ϕ`提议N个未来标记，云端用目标解码器验证并提交接受标记。
   - 草稿长度N设备自适应（CPU/边缘设备用短草稿，GPU用长草稿）。
4. **设备自适应执行**：
   - CPU：分块流式传输（`N_b`块），重叠解密与计算。
   - GPU：模型切片、张量并行、本地批处理。
   - 边缘设备：ONNX Runtime + 量化部署。
5. **传输保护与隐藏状态保护**：
   - 张量元素量化到b位（实验中8-bit），用AES-GCM加密。
   - 端点本地LoRA模块（低秩更新`W=W0+BA`）、草稿参数、局部投影权重不传输到云端。
   - KV缓存授权由端点控制（可禁用或限制窗口长度）。

### 公式与算法流程（文字说明）
- 嵌入计算：`h0 = Etok(u) + Epos(p) + Etype(t)`
- 控制元组：`c = (flag_Cache, L_Cache, d_type, ℓ)`
- 云解码器层：计算QKV，注意力，FFN，返回隐藏状态。
- 部分logits融合与采样公式（14-15）：对词汇子集元素相加，其余置-∞，softmax采样。
- 算法1：端点授权协作解码（预处理→发送特征→循环生成→验证→更新缓存）。

## 3. 实验设计

### 数据集/场景
- 使用1000个提示：400个中文提示、400个英文提示、200个混合语言提示。
- 评估任务：next-token一致性、困惑度（PPL）、MMLU（5-shot）、GSM8K（贪心思维链）、HumanEval pass@1（确定性解码）。

### Benchmark / 对比方法
- 四个基线：
  1. **Cloud-only**：端点发送原始提示+对话标识，云端完成所有推理。
  2. **Cloud-only encrypted transport**：同上但传输加密（仅保护信道）。
  3. **Naive split inference**：端点计算嵌入，云端返回隐藏状态或完整logits，无缓存授权、语言自适应、投机解码。
  4. **Endpoint-only lightweight**：紧凑本地模型（仅GPU/边缘轮廓可行）。

### 实验设置
- 目标模型：7B类解码器Transformer，隐藏维度D=4096，词汇量V=150,000。端点处理D_end=96，云端D_cloud=4000。
- 提示截断/填充至512 token，生成128 token响应。
- Tensor和logits量化到8-bit，AES-GCM加密。
- 网络条件：20ms RTT，2ms jitter，100 Mbps带宽限制。

## 4. 资源与算力

- **云端**：NVIDIA H20 96GB GPU，64 vCPU主机，512GB RAM（PyTorch服务）。
- **CPU端点**：12核x86 CPU，32GB RAM，无独立GPU。
- **GPU端点**：消费级GPU，8GB VRAM，32GB主机RAM。
- **边缘端点**：嵌入式ARM设备，8GB统一内存。
- **运行时间**：模型加载和初始化时间排除，实验重复5次，预热100个提示后统计。
- **训练时长**：未明确说明训练时间（LoRA模块适配时间？），但未报告。

## 5. 实验数量与充分性

- **核心实验**：
  - 端到端延迟对比（表3）：3种端点×4基线+本文 = 15组×5次重复。
  - 吞吐量和下行负载（表4）：3种端点×3语言模式 = 9组。
  - 消融实验（表5）：7种变体。
  - 敏感性分析（表6-7）：D_end（5种）、N（6种）。
  - 生成质量对比（表8）：4种方法。
  - 隐私暴露会计（表9）：5种方法定性比较。
- **充分性评估**：
  - 覆盖了主要研究问题（延迟、带宽、组件贡献、隐私暴露）。
  - 消融实验系统，敏感性分析合理。
  - 生成质量评估包含多个标准基准（MMLU、GSM8K、HumanEval）。
  - 但缺少对更大模型（如13B、70B）或更极端网络条件的测试。仅测试了128 token响应，长序列生成（如2048 token）未评估。
  - 隐私方面仅定性表格，未进行实际重构攻击评估。

## 6. 论文的主要结论与发现

- **延迟**：提议框架在三种端点上比朴素分割推理降低延迟29.4%/46.1%/26.8%，每token延迟降低最多46.1%。
- **通信**：在英文-only模式下，下行负载降低67.4%（vs 完整logits传输）；中文-only降低约56-57%。
- **生成质量**：next-token一致性98.6%，PPL 7.87，MMLU 54.3，GSM8K 49.4，HumanEval 27.1，与全云模型差距很小（全云：54.6/49.8/27.4）。
- **组件贡献**：投机解码和语言自适应压缩贡献最大；拆除它们导致延迟/带宽明显恶化。
- **隐私暴露**：相比cloud-only和朴素分割，本框架避免传输原始提示、明文历史ID、完整logits和端点私有模块。

## 7. 优点

1. **新颖的协作架构**：将缓存授权、分布式词汇投影、投机解码和语言自适应压缩有机整合，首次以隐私暴露为第一类目标设计端云LLM推理系统。
2. **全面的设备适配**：支持CPU（分块流式）、GPU（模型切片+批处理）和边缘设备（ONNX Runtime+量化），方案实用。
3. **系统级隐私保护**：结合传输级加密（AES-GCM）、端点私有模块（LoRA权重不上云）和缓存授权，而非单一机制。
4. **实验设计扎实**：覆盖延迟、吞吐、通信、消融、敏感性、生成质量和隐私暴露会计，对比基线合理。
5. **生成质量保留良好**：与全云模型差距极小（<2%），验证了设计在效率与质量间的平衡。

## 8. 不足与局限

1. **隐私保护非形式化**：论文明确指出不提供正式差分隐私或安全多方计算保证。隐藏状态仍可能被强对手重构（如模型反转攻击）。仅加密传输和保留本地模块是“实践减少暴露”而非数学保证。
2. **实验规模有限**：
   - 仅测试7B模型，更大模型（如13B/70B）未验证。
   - 网络条件单一（20ms RTT, 100Mbps），未模拟高延迟、低带宽或丢包场景。
   - 生成长度仅128 token，长序列生成（如对话历史复用）未评估。
3. **缺少真实用户场景隐私评估**：未进行实际的成员推断或重构攻击实验来量化隐私泄露程度（表格9仅定性）。
4. **实现假设**：端点信任（用户/本地应用控制），云端“诚实但好奇”。若云端恶意图谋，可能仍能通过观察张量进行隐私推断。
5. **超参数选择可能过拟合**：D_end=96, N=5基于当前硬件选取，可能在其他配置下非最优。
6. **未能与安全多方计算或可信执行环境方案对比开销**，仅与朴素分割和云端对比。

（完）
