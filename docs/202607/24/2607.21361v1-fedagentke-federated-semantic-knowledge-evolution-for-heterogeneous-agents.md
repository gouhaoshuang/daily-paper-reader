---
title: "FedAgentKE: Federated Semantic Knowledge Evolution for Heterogeneous Agents"
title_zh: FedAgentKE：面向异构智能体的联邦语义知识演化
authors: "Weihao Li, Jun Bai, Ziyang Song"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21361v1"
tags: ["query:eca"]
score: 8.0
evidence: 异构分布式智能体的联邦知识演化
tldr: 现有LLM智能体框架各自孤立运行，本地经验虽通过记忆系统有所改善，但跨框架知识迁移仍严重受限，阻碍协作推理演化。提出FedAgentKE轻量级联邦框架，通过迭代语义蒸馏从本地轨迹中提取可迁移推理抽象，经聚合形成全局知识库，再让各框架自适应调整实现知识进化，全程无需共享原始数据。在跨框架与跨任务实验中，智能体性能持续一致提升。该方法为构建隐私保护、协同演化的异构智能体生态系统提供了新范式，拓展了联邦学习在分布式智能体领域的应用前景。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21361v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1480, \"height\": 736, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21361v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 788, \"height\": 236, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21361v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 761, \"height\": 289, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21361v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 792, \"height\": 432, \"label\": \"Table\"}]"
motivation: 现有智能体框架孤立运行，本地经验碎片化，缺乏跨框架知识迁移机制，限制协同推理演化。
method: FedAgentKE通过迭代语义蒸馏、聚合与自适应调整，在异构智能体间协作进化可迁移推理抽象，无需共享原始轨迹。
result: 在跨框架与跨任务场景下，智能体性能均获得一致性显著提升，验证了联邦语义知识进化的有效性。
conclusion: 联邦语义知识进化能突破隔离壁垒，为未来大规模协作智能体生态系统提供可扩展、隐私保护的知识演化方案。
---

## 摘要
基于大语言模型的智能体越来越依赖推理、工具使用和迭代执行，然而现有的智能体框架在很大程度上仍然孤立运行。尽管近期基于记忆的智能体系统通过本地检索和工作流重用来改善单个智能体，但本地经验仍然分散在孤立的智能体框架之间，限制了跨框架的知识迁移和协作推理演化。我们提出FedAgentKE，一个用于跨异构智能体的联邦语义知识演化的轻量级框架。FedAgentKE使得分布式智能体框架能够通过迭代的语义知识蒸馏、聚合和适应来协作演化可迁移的推理抽象，而无需共享原始推理轨迹。实验表明，在跨框架和跨任务场景下均取得了一致的改进，凸显了联邦语义知识演化对未来协作智能体生态系统的潜力。

## Abstract
Large language model (LLM)-based agents increasingly rely on reasoning, tool use, and iterative execution, yet existing agent frameworks still operate largely in isolation. While recent memory-based agent systems improve individual agents through local retrieval and workflow reuse, local experiences remain fragmented across isolated agent frameworks, limiting cross-framework knowledge transfer and collaborative reasoning evolution. We propose FedAgentKE, a lightweight framework for Federated Semantic Knowledge Evolution across heterogeneous agents. FedAgentKE enables distributed agent frameworks to collaboratively evolve transferable reasoning abstractions through iterative semantic knowledge distillation, aggregation, and adaptation without sharing raw reasoning trajectories. Experiments demonstrate consistent improvements under both cross-framework and cross-task settings, highlighting the potential of federated semantic knowledge evolution for future collaborative agent ecosystems.

---

## 论文详细总结（自动生成）

# 中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有基于大语言模型（LLM）的智能体框架（如OpenHands、OWL、SmolAgents等）在推理、工具使用和迭代执行方面表现优异，但它们**独立运行，本地经验碎片化**，缺乏跨框架的知识迁移与协作推理演化机制。不同智能体往往重复发现相似解决方案、重复可避免的错误，限制了整体智能的进化。
- **研究背景**：虽然已有记忆增强系统（如Reflexion、A-MEM、Agent KB）尝试通过本地记忆重放或集中式知识库改善单个智能体，但它们仍主要依赖本地记忆或中心化存储，对**跨异构框架的分布式知识同步与自适应**支持不足。这促使将联邦学习（FL）思想引入智能体协作，但直接应用FL面临智能体推理协议、工具环境、执行工作流异构的挑战，且许多可迁移能力体现为可重用的推理抽象而非神经网络参数。
- **整体含义**：FedAgentKE旨在探索一种**联邦语义知识演化**方法，使分布式异构智能体在不共享原始执行轨迹的前提下，通过可迁移的语义知识单元实现协作推理能力的协同进化，为构建隐私保护、可扩展的协作智能体生态系统提供新范式。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
### 核心思想
- FedAgentKE将异构智能体协作形式化为**联邦语义知识演化问题**：分布式智能体客户端通过迭代的**本地语义蒸馏、联邦语义聚合、知识自适应**三个步骤，在不暴露原始推理轨迹的情况下同步可迁移的推理抽象。

### 关键技术细节
- **本地知识蒸馏**：
  - 每个客户端将本地执行轨迹 \( \tau_{i,j} = (x_{i,j}, a_{1:T_{i,j}}, o_{1:T_{i,j}}, y_{i,j}) \) 通过LLM蒸馏算子 \( \mathcal{D}_i \) 转化为可传输的语义知识单元 \( z_{i,j} = (\pi_{i,j}, \rho_{i,j}, e_{i,j}, c_{i,j}, u_{i,j}) \)，分别对应抽象任务描述、可重用推理工作流、纠错/失败模式、框架元数据、效用分数。
  - 该过程过滤了框架特定的实现细节，保留可迁移的推理模式与执行经验。

- **联邦语义聚合**（服务器端）：
  - 收集所有客户端上传的知识单元形成池 \( \mathcal{U}^{(t)} \)。
  - 通过语义嵌入函数 \( \phi(\cdot) \) 将知识单元编码为向量 \( h_z \)，然后进行**跨任务语义聚类**，将语义相关的推理抽象分组。
  - 对每个聚类 \( G_m \)，选择代表知识单元：\( \bar{z}_m = \arg\max_{z \in G_m} [\lambda_1 u(z) + \lambda_2 q(z)] \)，其中 \( u(z) \) 衡量经验效用，\( q(z) \) 衡量跨智能体/任务的可迁移性。
  - 通过语义去重和效用感知更新，将代表性知识整合进全局知识池 \( \mathcal{K}_g^{(t+1)} \)。

- **知识自适应**（客户端）：
  - 给定本地任务 \( x_i \)，客户端检索全局知识池中top-\(k\) 最相关的单元：\( \hat{\mathcal{K}}_i = \text{TopK}_{z \in \mathcal{K}_g} \text{sim}(\psi(x_i), \phi(z)) \)。
  - 通过自适应算子 \( \mathcal{T}_i \) 将联邦知识映射为本地可执行的推理指导 \( \tilde{\mathcal{K}}_i \)，再通过本地推理精炼算子 \( \mathcal{R}_i \) 更新本地工作流 \( \rho_i' = \mathcal{R}_i(\rho_i, \tilde{\mathcal{K}}_i) \)。

### 算法流程（文字说明）
- 循环 \( t=0 \) 至 \( T-1 \)：
  - 服务器分发当前全局知识池 \( \mathcal{K}_g^{(t)} \)。
  - 各客户端并行：检索知识 → 自适应 → 精炼本地推理 → 执行本地任务收集轨迹 → 蒸馏为知识单元 → 上传。
  - 服务器构建上传池，进行语义嵌入、聚类、代表性选择，更新全局知识池（效用指数平均更新、低效用知识替换）。

## 3. 实验设计：数据集、场景、基准、对比方法
- **数据集与任务**：
  - **GAIA**：通用多步推理与工具使用基准，包含Level-1（53个）、Level-2（86个）、Level-3（26个）任务。
  - **SWE-bench Lite**：软件工程真实GitHub问题解决任务，使用300个采样案例。
- **智能体框架（异构客户端）**：
  - OWL、SmolAgents、OpenHands、SWE-agent。
- **基准模型**：GPT-5.4-mini作为骨干模型（注：文章注明为未来模型，实际可能指代LLM）。
- **对比方法**：无联邦知识同步的**Standalone**（原始框架独立运行）。
- **实验场景**：
  - **同框架联邦**（Intra-framework）：同一框架下不同本地任务的客户端之间同步。
  - **跨框架联邦**（Cross-framework）：不同框架（OWL + SmolAgents）混合客户端，且变化客户端数量（2、4、8）。
  - **轮次演化**：观察不同通信轮次（R=1,2,5）的性能变化。

## 4. 资源与算力
- **未明确说明**：论文未提及使用的GPU型号、数量或训练时长。仅提到所有实验使用“GPT-5.4-mini”作为骨干模型，但未说明该模型的部署方式或计算资源。可能依赖外部API或虚拟模型（论文发布于2026年，可能为前瞻研究）。也没有报告通信成本或训练时间。

## 5. 实验数量与充分性
- **实验数量**：
  - 同框架联邦：2个框架（SWE-agent、OpenHands）× 2个轮次，GAIA上另有OWL和SmolAgents各3个难度级别。
  - 跨框架联邦：客户端规模2/4/8，报告全部三个GAIA级别+平均。
  - 轮次演化：SmolAgents在GAIA上R=1至5。
  - 附录中提供了一个流程级案例分析，展示了知识蒸馏到复用过程。
- **充分性与公平性**：
  - **优点**：覆盖了同框架、跨框架、多客户端、多轮次，GAIA分难度报告，结果清晰对比Standalone。
  - **不足**：
    - 缺少详细的**消融实验**（如不同聚类阈值、效用函数权重、知识单元数的影响）。
    - 没有与直接使用原始轨迹、中心化知识库（如Agent KB）等方法对比，仅对比Standalone。
    - 跨框架实验中仅使用OWL和SmolAgents两种，未包含OpenHands/SWE-agent的异构组合。
    - 缺少对**隐私泄露风险、通信开销、知识冗余**的量化分析。
    - 基准模型为GPT-5.4-mini，其性能是否代表真实LLM能力未知（可能为合成模型）。
  - 总体而言，实验设计合理但覆盖范围有限，结果统计意义需更多验证。

## 6. 论文的主要结论与发现
- **同框架联邦**：FedAgentKE在SWE-bench Lite上对SWE-agent和OpenHands分别提升15.67和15.34个百分点（R=2）；在GAIA上对OWL提升19.39、对SmolAgents提升13.34（平均）。
- **跨框架联邦**：随着异构客户端数量增加（2→8），GAIA平均成功率从49.0%提升至54.4%，表明更多样化的协作知识有助于演化。
- **轮次迭代**：SmolAgents在GAIA上R=1时44.24%，R=5时达到74.55%，说明迭代联邦同步持续提升可迁移推理能力。
- **主要结论**：联邦语义知识演化能够在不共享原始轨迹的前提下，有效实现跨异构框架的知识迁移与协作改进，且具有可扩展性。

## 7. 优点：方法或实验设计上的亮点
- **方法亮点**：
  - **语义层面操作**：将原始轨迹蒸馏为可迁移的语义知识单元，而非参数级聚合，天然适应异构框架。
  - **隐私友好**：无需共享原始数据，仅共享抽象知识。
  - **轻量级**：无需修改智能体框架内部逻辑，只需在外部添加蒸馏/适配模块。
  - **任务感知聚合**：通过语义聚类实现跨任务知识复用，减少冗余。
  - **效用与可迁移性双重选择**：保留了最有效的知识，动态更新效用分数。
- **实验亮点**：
  - 覆盖了同框架、跨框架、多轮次、多客户端规模，结果清晰。
  - 提供了流程级案例（附录D），直观展示失败→蒸馏→聚合→修正的全过程。

## 8. 不足与局限
- **实验覆盖不足**：
  - 未与其他现有的跨框架知识共享方法（如Agent KB）对比。
  - 跨框架实验中仅涉及OWL和SmolAgents，未测试更多异构组合（如OpenHands+SWE-agent）。
  - 消融实验缺失（如蒸馏粒度、聚类阈值、知识容量等）。
- **可迁移性验证不充分**：仅在一个平均数据集上进行跨框架测试，未在更多样化的任务域（如Web交互、问答）验证。
- **隐私与效率未建模**：论文明确指出未显式建模通信效率或隐私保护机制，语义知识单元本身可能仍泄露部分模式。
- **嵌入机制局限**：当前基于嵌入的语义更新方法可能无法捕获复杂的推理依赖关系，尤其当知识依赖上下文时。
- **规模限制**：仅在3个客户端（统一框架联邦）或最多8个客户端（跨框架）上测试，大规模生态下的推理成本、收敛性、知识冲突未验证。
- **骨干模型问题**：使用GPT-5.4-mini（可能为虚构），该模型性能与现有主流LLM（如GPT-4、Claude）的差距未知，导致结论的泛化性受限于假设模型。

（完）
